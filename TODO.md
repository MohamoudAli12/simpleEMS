- Gerber export layer by layer. implement layering system
- ~parameter sweep should return results~ 
- ~remove export gerber from __all__ simtools~
- add a function called `simulate_model` where it takes CSX geometry and calls openEMS to simulate it. saw this in a dream
- change function to named fields

